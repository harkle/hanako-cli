// Autogenerated by Hanako

<% props.modules.forEach(function(module) { %>
import { <%= module.className %> } from '../../<%= module.path %>';<% }); %> 

<% props.modules.forEach(function(module) { %>
(new <%= module.className %>()).init();<% }); %> 
